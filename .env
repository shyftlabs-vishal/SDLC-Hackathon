# >>> continuum managed >>>
# Managed by `continuum up minimal` — edits inside this block are overwritten.
VECTOR_STORE_PROVIDER=qdrant
LANGFUSE_ENABLED=false
TEMPORAL_ENABLED=false
# <<< continuum managed <<<
